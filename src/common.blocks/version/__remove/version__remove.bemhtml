block('version').elem('remove')(
    content()(function() {
        return {
            block : 'button',
            url : this.ctx.url,
            text : 'Удалить',
            mods : { theme : 'normal', size : 's', type : 'link', view: 'action' }
        };
    })
);
