block('content-wrapper')(
//    def()(function() {
//        var params = req.params,
//            lib = params.lib,
//            version = params.version;
//
//        if(lib && version) {
//            this.extend(this.ctx, { mods: { view: version } });
//            applyNext();
//        }
//    }),

    content()(function() {
        return {
            block: 'panel',
            mods: { type: 'make' }
        };
    })
);
