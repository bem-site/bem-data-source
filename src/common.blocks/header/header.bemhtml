block('header').content()(function() {
    return [
        {
            block : 'button',
            url : '#',
            text : 'Создать сборку',
            mods : { theme : 'normal', size : 'm', type : 'link', view: 'action' }
        },
        { elem: 'title' }
    ];
})
