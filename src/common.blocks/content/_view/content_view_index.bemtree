block('content').mod('view', 'index')(
    content()(function() {
        var data = this.ctx.data;
        return [
            {
                block: 'menu-wrapper',
                title: 'Библиотеки',
                items: data.libraries
            }
        ];
    })
);
